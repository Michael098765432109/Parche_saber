Necesito que prepares mi archivo `index.html` para subir mi proyecto a PRODUCCIÓN.

IMPORTANTE: NO quiero que reconstruyas el proyecto desde cero, no cambies la lógica existente y no cambies el diseño visual.

OBJETIVO PRINCIPAL

Revisa y modifica únicamente lo necesario para que mi `index.html` quede correctamente preparado para producción y funcione con mi configuración actual de Supabase.

El proyecto actualmente funciona y tiene lógica ya implementada. El trabajo consiste en una adaptación técnica para producción, no en un rediseño ni una refactorización arbitraria.

REGLAS OBLIGATORIAS

1. NO cambiar la lógica

Conserva exactamente el comportamiento actual de login, registro, cierre de sesión, persistencia de sesión, perfiles de estudiante y docente, nombre y apellido, grado y sección, preguntas, tests, evaluaciones, simulacros, materiales, contenidos, resultados, intentos, roles, permisos, filtros por grado y sección, y todas las funciones conectadas a Supabase.

No elimines funciones que actualmente funcionan.
No reemplaces Supabase por localStorage, Firebase, IndexedDB ni otra solución.

2. NO cambiar el diseño

Conserva colores, gradientes, tipografías, tamaños, espaciados, botones, tarjetas, menús, modales, formularios, iconos, animaciones, responsive, estructura visual y clases CSS existentes.

No hagas un rediseño.

3. Supabase

Verifica la URL, la clave pública, la inicialización del cliente, supabase.auth, sesiones, consultas, inserciones, actualizaciones, eliminaciones, Storage, usuarios autenticados y manejo de errores.

Nunca expongas una service_role key en el frontend. Si existe una clave pública o anon, mantenla como corresponde para una aplicación frontend.

4. Producción

Comprueba rutas relativas, archivos JS y CSS, imágenes, fuentes, iconos, imports, scripts externos, URLs, dependencias CDN y referencias a localhost, 127.0.0.1, desarrollo, pruebas, datos demo o datos ficticios.

5. Seguridad

Busca claves privadas, service_role, contraseñas, tokens hardcodeados, datos sensibles, credenciales de prueba, consultas inseguras, XSS y manejo incorrecto de sesiones. Si una corrección requiere modificar Supabase, RLS, políticas o configuración externa, no la inventes ni la modifiques desde index.html.

6. NO inventar información

No inventes URLs, keys, tablas, columnas, IDs, buckets, políticas RLS, variables de entorno ni funciones. Utiliza exclusivamente la información existente en el proyecto.

7. Código

Antes de modificar el archivo, analiza index.html y sus dependencias, la conexión con Supabase, las partes de desarrollo y producción, posibles errores, funciones duplicadas, referencias rotas y eventos.

Después realiza únicamente los cambios necesarios.

8. No hacer refactorización innecesaria

No conviertas el proyecto a otro framework, no cambies JavaScript por TypeScript, no cambies Supabase, no reorganices el código por estética, no elimines código funcional y no cambies nombres de funciones, IDs, clases ni estructuras de datos salvo que sea estrictamente necesario.

CRITERIO DE ACEPTACIÓN

Al finalizar, index.html debe mantener la apariencia, lógica y funcionalidades existentes, seguir utilizando Supabase, ejecutarse en producción, no depender de localhost ni de datos demo, no contener credenciales privadas, cargar correctamente sus dependencias, tener rutas válidas y no presentar errores evidentes de JavaScript.

Si hace falta modificar otros archivos además de index.html, indica cuál, por qué, qué problema solucionaría y si es obligatorio o recomendable antes de modificarlos.

ENTREGA

Entrega el index.html completo y corregido, sin omitir partes ni usar puntos suspensivos. Indica los cambios realizados y cualquier configuración pendiente en Supabase o el hosting.

Prioridad absoluta: funcionalidad actual, compatibilidad con producción, seguridad y limpieza del código. El diseño visual y la lógica existente deben permanecer intactos.
